<div class="container">
  <h3>Post A Comment</h3>

  <%= form_for [ @article, @comment ] do |f| %>
    <p>
      <%= f.label :author_name, "your name, duh" %><br/>
      <%= f.text_field :author_name %>
    </p>
    <p>
      <%= f.label :body, "your comment"%><br/>
      <%= f.text_area :body %>
    </p>
    <p>
      <%= f.submit 'Submit' %>
    </p>
  <% end %>
</div>
